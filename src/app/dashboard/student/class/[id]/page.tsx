'use client';

export default function ClassDetailsPage({ params }: { params: { id: string } }) {
  return (
    <div className="bg-white p-4">
      {/* Same content as ClassDetails component */}
      {/* Class info, students, materials */}
    </div>
  );
} 