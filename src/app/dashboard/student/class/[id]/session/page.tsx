'use client';

export default function ClassSessionPage({ params }: { params: { id: string } }) {
  return (
    <div className="bg-white p-4">
      {/* Video call interface */}
      {/* Chat and participants */}
    </div>
  );
} 